# Diagramas

Los diagramas del proyecto se mantienen en `diagram-design`.

Regla:

- Fuente: `.html` autocontenida.
- Render: `.svg` o `.png`.
- No usar Mermaid ni draw.io como formato objetivo.

Primer diagrama pendiente:

- `mapa-autoridad-de-estado.html`
- `mapa-autoridad-de-estado.svg`
